# Substrate-only guest helpers. Product appliance iteration happens in the
# downstream product flake, which may override its nix-on-rocks input locally.

build target="cemu":
    nix build ".#{{target}}"

flake-show:
    nix flake show --all-systems .

static-checks:
    ./scripts/static-checks.sh
